#include <iostream>

using namespace std;

//Codigo não finalizado

int main(){
    /*
    int hora_entrada, min_entrada, hora_saida, min_saida;
    float preco = 5.0,intervalo;

    cout << "Hora de entrada: ";
    cin >> hora_entrada;

    cout << "Minutos: ";
    cin >> min_entrada;

    cout << "Hora de Saída: ";
    cin >> hora_saida;

    cout << "Minuto: ";
    cin >> min_saida;

    intervalo = ((hora_entrada * 60) + min_entrada) - ((hora_saida * 60) + min_saida);

    intervalo =  ((intervalo + 30) / 60) * 60;

    if (intervalo <= 120){
        preco = 1.0;
    }
    else if ((intervalo > 120) && (intervalo <=240)){
        preco = 1.4;
    }
   

    cout << "Valor a pagar: " << (preco * intervalo) * (-1) << endl;

    return 0;   
    */
}